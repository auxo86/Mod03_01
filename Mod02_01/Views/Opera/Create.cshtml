@*產生強型別的基礎*@
@model Mod02_01.Models.Opera

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Create</title>
</head>
<body>
    <div>
        @using (Html.BeginForm())
        {
            <fieldset>
                <legend>新增歌劇</legend>
                <div>@Html.LabelFor(m => m.Title)</div>
                <div>@Html.EditorFor(m => m.Title)</div>
                <div>@Html.LabelFor(m => m.Year)</div>
                <div>@Html.EditorFor(m => m.Year)</div>
                <div>@Html.LabelFor(m => m.Composer)</div>
                <div>@Html.EditorFor(m => m.Composer)</div>
                <div>
                    <input type="submit" value="新增歌劇"/>
                </div>
            </fieldset>
        }
        <p>
            @Html.ActionLink("回歌劇列表", "Index")
        </p>
    </div>
</body>
</html>
